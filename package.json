{
  "name": "bouquetbuilder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "dev": "cd frontend && npm run dev",
    "deploy": "cd frontend && npm install --legacy-peer-deps && VITE_API_URL=https://bouquetbuilder.onrender.com VITE_CHATBOT_URL=https://bouquetbuilder-chatbot.onrender.com npm run build && cd .. && npx wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
